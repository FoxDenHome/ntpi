# Append -P 22 --device=/dev/ttyAMA0 to all

# Examples for correct ubxtool config (might need -s BAUDRATE override)
ubxtool -S 115200
ubxtool -e BINARY
ubxtool -d NMEA
ubxtool -p MODEL,2 # 2 = stationary
ubxtool -p SAVE

# Survey for accurate time at constant location
ubxtool -e SURVEYIN
# wait until done, check with "-p TIM-SVIN | grep -A3 'UBX-TIM-SVIN" (active must be 0, valid must be 1 on UBX-TIM-SVIN)
ubxtool -p SAVE
