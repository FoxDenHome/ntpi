# Append --device=/dev/ttyAMA0 to all

# Examples for correct ubxtool config (might need -s BAUDRATE override)
ubxtool -S 115200
ubxtool -e NMEA
ubxtool -d BINARY
ubxtool -p SAVE

# Survey for accurate time at constant location
ubxtool -e SURVEYIN
# wait until done, check with "ubxtool -p TIM-SVIN" (active must be 0, valid must be 1 on UBX-TIM-SVIN)
ubxtool -p SAVE
